name: C/C++ CI

on:
    push:
      branches: [ local ]
    pull_request:
      branches: [ local ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: run-cmake
      uses: lukka/run-cmake@v2.0
